@model God

<h2>Create a New God</h2>

@using (Html.BeginForm("InsertGodToDatabase", "God", "Post"))
{
    <input asp-for="GodID" type="hidden" value="" />

    <label asp-for="Name" class="control-label">Name</label>
    <input type="text" name="Name" class="form-control" value="" />

    <label asp-for="Description" class="control-label">Description</label>
    <input type="text" name="Description" class="form-control" />
    
    <label asp-for="Temple_Location" class="control-label">Temple Location</label>
    <input type="text" name="Temple_Location" class="form-control" />
    
    <label asp-for="Parents" class="control-label">Parents</label>
    <input type="text" name="Parents" class="form-control" />

   @* <div class="form-group">
        <label for="">Select a Category</label>
        <select class="form-control" name="CategoryID" value="">
            @foreach (var cat in Model.Categories)
            {
                <option value="@cat.CategoryID">@cat.CategoryID @cat.Name</option>
            }
        </select>
    </div>*@

    <input type="submit" value="Create God" class="btn btn-primary" />
}